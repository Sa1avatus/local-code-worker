{
  "task_id": "vacancy-deduplication-001",
  "title": "Add vacancy deduplication",
  "goal": "Prevent duplicate vacancies from being created",
  "repository_root": "D:/projects/job-agent",
  "allowed_files": [
    "src/vacancies/service.py",
    "src/vacancies/repository.py",
    "tests/vacancies/test_service.py"
  ],
  "readonly_files": [
    "src/vacancies/models.py",
    "docs/ARCHITECTURE.md"
  ],
  "requirements": [
    "Match vacancies by provider and external_id",
    "Preserve created_at when updating",
    "Update updated_at"
  ],
  "interfaces": [
    {
      "name": "get_by_provider_and_external_id",
      "signature": "async def get_by_provider_and_external_id(self, provider: VacancyProvider, external_id: str) -> Vacancy | None"
    }
  ],
  "acceptance_criteria": [
    "Repeated import does not create a duplicate row",
    "Existing internal ID is preserved",
    "Tests pass"
  ],
  "validation_commands": [
    ["python", "-m", "ruff", "check", "src/vacancies", "tests/vacancies"],
    ["python", "-m", "pytest", "tests/vacancies/test_service.py", "-q"]
  ],
  "max_context_characters": 50000,
  "max_output_characters": 100000
}
